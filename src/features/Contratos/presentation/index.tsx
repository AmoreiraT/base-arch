import PageTitle from '../../../shared/components/PageTitle';
import * as S from './styles';

const Contratos = (): JSX.Element => {
  return (
    <S.Container id="contratos">
      <PageTitle title={'Contratos'} />
    </S.Container>
  );
};

export default Contratos;
