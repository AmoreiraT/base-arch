import PageTitle from '../../../shared/components/PageTitle';
import * as S from './styles';

const Publicacoes = (): JSX.Element => {
  return (
    <S.Container id="publicacoes">
      <PageTitle title={'Publicações'} />
    </S.Container>
  );
};

export default Publicacoes;
